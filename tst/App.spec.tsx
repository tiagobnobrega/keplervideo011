/**
 * @format
 */

import 'react-native';

jest.useFakeTimers();

/**
 *TODO: remove skip to add test case
 */

// eslint-disable-next-line jest/no-disabled-tests
it.skip('Renders Correctly', () => {});
