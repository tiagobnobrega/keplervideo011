import { Dimensions } from 'react-native';

//AutoRotator spin cycle duration in ms
export const MINUTE_MS = 5000;

//Get screen width and height
export const { width, height } = Dimensions.get('window');
